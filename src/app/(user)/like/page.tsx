import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "LikePage",
    description: "LikePageTIen",
};

const LikePage = () => {
    return <div>Like Page</div>;
};

export default LikePage;
